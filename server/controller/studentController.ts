import type { NextFunction, Request, Response } from "express";

export class stundentController {

    getStudents (req : Request, Res : Response, next : NextFunction) {

    }
    addStudents (req : Request, Res : Response, next : NextFunction) {

    }
    deleteStudents (req : Request, Res : Response, next : NextFunction) {

    }
    updateStudents (req : Request, Res : Response, next : NextFunction) {

    }
}